import { styled } from "@mui/system";

export const TotalWrapper = styled("div")({
  display: "flex",
  justifyContent: "space-between",
  fontWeight: "bold",
  padding: "0 8px",
  marginBottom: "24px",
});
